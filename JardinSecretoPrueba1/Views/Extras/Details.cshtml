@model JardinSecretoPrueba1.Models.Extra

@{
    ViewData["Title"] = "Detalles del Extra";
}

<div class="container mt-5">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-info text-white d-flex justify-content-between align-items-center">
            <h4 class="mb-0"><i class="fa-solid fa-eye me-2"></i>Detalles del Extra</h4>
            <a asp-action="Index" class="btn btn-light btn-sm">
                <i class="fa-solid fa-arrow-left me-1"></i> Volver a Extras
            </a>
        </div>

        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Nombre</dt>
                <dd class="col-sm-9">@Model.Nombre</dd>

                <dt class="col-sm-3">Precio</dt>
                <dd class="col-sm-9">$@Model.PrecioExtra</dd>

                <dt class="col-sm-3">Producto</dt>
                <dd class="col-sm-9">@Model.IdProductoNavigation.Nombre</dd>
            </dl>

            <div class="text-end">
                <a asp-action="Edit" asp-route-id="@Model.ExtraId" class="btn btn-primary btn-sm">
                    <i class="fa-solid fa-pen me-1"></i> Editar
                </a>
                <a asp-action="Index" class="btn btn-secondary btn-sm">
                    <i class="fa-solid fa-arrow-left me-1"></i> Volver
                </a>
            </div>
        </div>
    </div>
</div>
